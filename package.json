{
  "name": "assignment-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend/paste-bin && npm install && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend/paste-bin && npm install"
  },
  "dependencies": {}
}